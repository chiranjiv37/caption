# Frontend Task

You are working inside the Next.js frontend.

Priorities:

* reuse hooks
* reuse components
* use Tailwind
* use shadcn/ui
* use existing API client

Avoid:

* duplicated JSX
* duplicated API calls
* unnecessary Context
* inline business logic

Follow existing UI patterns.
